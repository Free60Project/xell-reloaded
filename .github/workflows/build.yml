name: build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    container: free60/libxenon:latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make
